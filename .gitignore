# Environment — never commit real API keys
.env

# SQLite database — runtime data, not source code
backend/alphaglyph.db

# ML training data — rebuilt by ml/dataset.py, too large for git
# (the exported backend/ml_model.onnx + meta JSON ARE committed)
ml/data/

# Personal interview/essay prep — not for the public repo
STUDY_GUIDE.md

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
env/

# OS
.DS_Store
Thumbs.db
